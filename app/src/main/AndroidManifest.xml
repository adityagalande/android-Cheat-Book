<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cheatbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CheatBook">
        <activity android:name=".About"></activity>
        <activity android:name=".FeedbackAndContactUs" />
        <activity android:name=".RequestGame" />
        <activity android:name=".DetailCustomLayout" />
        <activity android:name=".XboxActivity" />
        <activity android:name=".PS4Activity" />
        <activity android:name=".PS3Activity" />
        <activity android:name=".PCActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>